# 开发留痕

## 2026-04-21 阶段记录（初始化骨架）

### 完成项

- 初始化 Go 模块：`github.com/yclenove/replpilot`
- 建立 Cobra 根命令与核心子命令占位：
  - `preflight`
  - `bootstrap`
  - `status`
  - `diagnose`
- 建立基础文档：
  - 架构设计
  - 开发文档
  - README

### 关键决策

- 控制端采用跨端 CLI（Linux/Windows/macOS），目标机先聚焦 Linux。
- 用户体验采用三步闭环：`preflight -> bootstrap -> status/diagnose`。

### 遗留问题

- 尚未实现 SSH、MySQL、状态机与审计模块。
- 尚未接入真实主从环境做联调。

## 2026-04-21 阶段记录（配置管理与环境就绪）

### 完成项

- 已安装 Go（Homebrew）并完成项目依赖拉取与测试基线验证。
- 增加本地配置存储模块：`internal/config/store.go`。
- 增加配置管理命令：
  - `replpilot init`
  - `replpilot host add/list`
  - `replpilot source add/list`

### 关键决策

- 当前配置文件采用 JSON（`~/.replpilot/config.json`），先保证稳定可用，后续可平滑迁移到 YAML。

### 遗留问题

- 尚未实现敏感信息安全存储（例如密码加密）。
- 尚未实现 `remove/update` 子命令与配置字段完整校验。

## 2026-04-21 阶段记录（preflight 初版落地）

### 完成项

- preflight 从占位逻辑升级为可执行检查链路。
- 新增配置查询方法：`FindHost`、`FindSource`。
- preflight 支持参数：`--source --replica --timeout`，并输出逐项 PASS/FAIL 结果。

### 当前远程检查项

- ssh 连通
- sudo 无密码能力（`sudo -n true`）
- mysql 客户端与版本

### 遗留问题

- 尚未接入真实 MySQL 权限细粒度校验（如 `REPLICATION SLAVE` 权限检查）。
- 尚未支持自动修复（`--fix` 仅提示未实现）。

## 2026-04-21 阶段记录（删除命令与网络预检查）

### 完成项

- 新增配置删除命令：`host remove`、`source remove`。
- preflight 增加“从库到主库端口连通”检查，支持 `nc` 与 `/dev/tcp` 双路径。

### 遗留问题

- 删除命令暂未增加“被任务引用时阻断删除”的保护策略。

## 2026-04-21 阶段记录（任务状态链路打通）

### 完成项

- 新增状态模块：`internal/state/task.go`。
- `bootstrap` 增加 `--dry-run` 并写入任务状态。
- `preflight` 结果写入状态文件，供诊断复用。
- `status/diagnose` 从状态文件读取并输出摘要建议。

### 关键决策

- 当前阶段优先打通“可观测与可追踪”链路，真实 MySQL 复制执行在下一阶段接入。

### 遗留问题

- `bootstrap --dry-run=false` 目前仍是编排骨架，尚未执行真实复制 SQL。
